function solution(arr){    arr.sort((first,second)=>{        const [firstStart, firstStop]= first        const [secondStart, secondStop]= second        return firstStart === secondStart ?firstStop - secondStop : firstStart - secondStart    })    let currentMeetingTime = [0,0]    let idx = 0    let todayMeetingCnt = 0    while(arr.length > idx){        const [nextMeetingStart, nextMeetingStop] = arr[idx]        const [currentMeetingStart, currentMeetingStop] = currentMeetingTime        if(nextMeetingStart >= currentMeetingStop){            currentMeetingTime = [nextMeetingStart, nextMeetingStop]            todayMeetingCnt++        }        idx++    }    return todayMeetingCnt}function solution(arr){    arr = arr.sort((first,second)=>{        const [firstStart, firstStop]= first        const [secondStart, secondStop]= second        return firstStop === secondStop ?firstStart - secondStart : firstStop - secondStop    })    let lastEndTime = 0    let idx = 0    let todayMeetingCnt = 0    while(arr.length > idx){        const [nextMeetingStart, nextMeetingStop] = arr[idx]        if(nextMeetingStart >= lastEndTime){            lastEndTime = nextMeetingStop            todayMeetingCnt++        }        idx++    }    return todayMeetingCnt}console.log(solution([[1,4],[2,6],[4,7]]))
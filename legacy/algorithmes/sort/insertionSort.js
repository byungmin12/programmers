/** * 맨 앞 +1 부터 아래로 비교하며 정렬하는 방 *중복 데이터가 있을 시 데이터의 위치를 교환하지 않고 지나가는 stable한 정렬 * */function insertionSort(arr){    for(let i =1; i< arr.length; i++ ){        let cur = arr[i];        let left = i -1;        while(left >=0 && arr[left] > cur){            arr[left+1] = arr[left]            left--        }        arr[left+1] = cur        console.log(`${i}회전: ${arr}`);    }    return arr}// insertionSort([5, 4, 3, 2, 1])// 결과 = [1, 2, 3, 4, 5]// [5, 4, 3, 2, 1]// 1회전: 4,5,3,2,1// 2회전: 3,4,5,2,1// 3회전: 2,3,4,5,1// 4회전: 1,2,3,4,5
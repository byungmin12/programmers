/** * 기점을 중심으로 반을 나누고 기점보다 작으면 왼쪽 크면 오른쪽으로 이동 * */function quickSort(arr,left=0,right=arr.length -1) {    if(left >= right)return;    const divide = (arr,left,right,centerValue) => {        while(left<=right){            while(arr[left] < centerValue){                left++            }            while(arr[right] > centerValue){                right--            }            if(left <= right){                [arr[left],arr[right]] = [arr[right],arr[left]]                left++                right--            }        }        return left    }    const mid = Math.floor((left+right)/2)    const centerValue = arr[mid]    const partition = divide(arr,left,right,centerValue)    quickSort(arr,left, partition-1)    quickSort(arr,partition, right)}let a = [1,12,5,26,7,14,3,7,2]quickSort(a)console.log(a)
function solution(citations) {//     if(citations.length ===1){//         return 1//     }//     const sortedCitations = citations.sort((tapeEquilibrium.js,b)=>b-tapeEquilibrium.js)//     for(let i =0, length = sortedCitations.length ; i<length ; i++){//         if(i+1 > sortedCitations[i]){//             return i//         }//     }    const sortedCitations = citations.sort((a,b)=>b-a)    let i = 0    while( i +1 <=  sortedCitations[i]){        i++    }    return i}
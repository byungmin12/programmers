const solution = (nums) => {    const hash = {}    const deletes = {}    nums.forEach((num)=>{        if(hash[num]=== undefined && deletes[num] === undefined){            hash[num] = 1        }else{            deletes[num] = true            delete hash[num]        }    })    const arr = Object.keys(hash)    return arr.length ===0 ? -1 : Math.max(...arr)}console.log(solution( [5,7,3,9,4,9,8,3,1,9])) //8console.log(solution( [9,9,8,8])) // -1
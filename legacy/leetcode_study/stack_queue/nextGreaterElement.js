/** * @param {number[]} nums1 * @param {number[]} nums2 * @return {number[]} */var nextGreaterElement = function(nums1, nums2) {    const store = nums2.reduce((acc,cur,idx)=>{        let nextValue = -1        for(let i =idx+1 ; i<nums2.length; i++){            if(cur < nums2[i]){                nextValue = nums2[i]                break            }        }        acc[cur] = nextValue        return acc    },{})    return nums1.map(num => store[num])};
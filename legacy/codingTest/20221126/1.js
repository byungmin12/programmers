function solution(flowers) {    //한 종류의 꽃이 피어있는 기간    //단 , 꽃이 지는 ㅏㄹ은 꽃이 피어있지 않은 것    let cnt =0    let prev    for(let flower of flowers){        if(prev && prev[1] >= flower[0]){            prev = [prev[0],flower[1]]            cnt = flower[1] - prev[0]        }else{            prev = flower            cnt = cnt + (flower[1] - flower[0])        }    }    return cnt}
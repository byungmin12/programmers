function solution (bracket){    let stack =[]    const isValid = function(s) {        for (let i = 0 ; i < s.length ; i++) {            let c = s.charAt(i);            switch(c) {                case '(': stack.push(')');                    break;                case '[': stack.push(']');                    break;                case '{': stack.push('}');                    break;                default:                    if (c !== stack.pop()) {                        return false;                    }            }        }        return stack.length === 0;    };    isValid(bracket)    const v = stack[0]    stack = []    console.log(stack,v)    let cnt = 0    for(let i = 0; i<bracket.length; i++){        const newStr = [bracket.slice(0,i),v,bracket.slice(i)].join("")        if(isValid(newStr)){            cnt++        }    }    return cnt}console.log(solution("(()()()"))
function solution(keymap, targets) {    // keymap을 해쉬로 변경    const keyMapHash = keymap.map((keys)=>{        return keys.split("").reduce((acc, cur, i) => {            acc[cur] = acc[cur] === undefined ? i + 1 : acc[cur]            return acc        }, {})    })    let result = []    for(let target of targets){        const splitTarget = target.split("").reduce((acc,cur)=>{            let minPos = Infinity;            keyMapHash.forEach((k)=>{                minPos =k[cur]===undefined ? minPos : Math.min(minPos, k[cur]);            })            return acc + minPos        },0)        result.push(splitTarget === Infinity ? -1 : splitTarget)    }    return result}
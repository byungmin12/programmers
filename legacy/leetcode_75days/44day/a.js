function solution(histogram) {    const length = histogram.length    const memo = Array(histogram[0].length).fill(0)    const isOne = Array(histogram[0].length).fill(false)    for(let i =0; i<length; i++){        const row = histogram[i]        for(let n=0; n<memo.length; n++){            const cell = row[n]            if(isOne[n]=== true)continue            if(cell ===0 && memo[n]> 0){                memo[n] = 0            }            if(cell ===1 && isOne[n] === false && memo[n] ===0 ){                isOne[n]= true                memo[n] = 1            }            if(cell===1 && isOne[n] === false  && memo[n] >0 ){                isOne[n]= true                memo[n]++            }            if(cell ===2 && isOne[n] === false){                memo[n]++            }        }    }    const result = memo.reduce((acc,cur,idx)=>{        return isOne[idx]=== false ? acc * (cur+1)  :  acc * cur    },1)    return result}
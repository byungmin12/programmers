const findMinArrowShots = function (points) {    const sortedPoints = points.sort((a, b) => a[0] + a[1] - (b[0] + b[1]))    let cnt = 0    let cur = [...sortedPoints[0]]    for (let i = 1; i < sortedPoints.length; i++) {        if (cur[0] <= sortedPoints[i][0] && sortedPoints[i][0] <= cur[1]) {            cur = [Math.max(cur[0],sortedPoints[i][0]), Math.min(cur[1],sortedPoints[i][1])]        } else if (sortedPoints[i][0] > cur[1]) {            cnt++            cur = [...sortedPoints[i]]        }    }    return cnt+1}
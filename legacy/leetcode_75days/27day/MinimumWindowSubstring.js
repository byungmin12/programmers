/** * @param {string} s * @param {string} t * @return {string} */var minWindow = function(s, t) {    let m = s.length    let n = t.length;    if(m<n)return ""    let min = ""    let checkObj = {}    new Array(n).fill(0).forEach((_,idx)=>{        checkObj[t[idx]] = checkObj[t[idx]] ? checkObj[t[idx]] +1 : 1    })    let cnt = Object.keys(checkObj).length    let left = 0;    let right = -1    while(right <= m){        if(cnt===0){            let cur = s[left]            if(checkObj[cur] !== undefined){                checkObj[cur]++            }            if(checkObj[cur] >0)cnt++            let temp = s.substring(left, right+1)            if (min == "") min = temp;            else min = min.length<temp.length?min:temp;            left ++        }else{            right++            let cur = s[right]            if(checkObj[cur] !== undefined){                checkObj[cur]--            }            if(checkObj[cur] ===0){                cnt--            }        }    }    return min};/** * @param {string} s * @param {string} t * @return {string} */var minWindow = function(s, t) {    let m = s.length    let n = t.length;    if(m<n)return ""    let min = s+"a"    let checkObj = {}    new Array(n).fill(0).forEach((_,idx)=>{        checkObj[t[idx]] = checkObj[t[idx]] ? checkObj[t[idx]] +1 : 1    })    for(let i =0; i<m-n +1; i++){        if(checkObj[s[i]] ===undefined)continue        let pointer= i        let cnt = n;        let str= ""        let localCheck = {...checkObj}        while(0< cnt && pointer<m ){            str= str + s[pointer]            if(str.length > min.length){                break            }            if(localCheck[s[pointer]]>0){                cnt--                localCheck[s[pointer]]--            }            if(cnt===0){                if(min.length > str.length){                    min = str                }                break            }            pointer++        }    }    return  min===s+"a" ? "":min};
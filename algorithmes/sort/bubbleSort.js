/** * 맨 앞에서부터 두배씩 비교하며 정렬하는 방식 * 중복 데이터가 있을 시 데이터의 위치를 교환하지 않고 지나가는 stable한 정렬 * */function bubbleSort(arr) {    for (const idx in arr) {        let swap;        for (let j = 0; j < arr.length - 1 - idx; j++) {            if (arr[j] > arr[j + 1]) {                swap = arr[j];                arr[j] = arr[j + 1]                arr[j + 1] = swap            }        }        console.log(`${idx}회전: ${arr}`);        if (!swap) {            break        }    }    return arr}console.log(bubbleSort([5, 4, 3, 2, 1]))// 결과 =  [1, 2, 3, 4, 5]// [5, 4, 3, 2, 1]// 0회전: 4,3,2,1,5// 1회전: 3,2,1,4,5// 2회전: 2,1,3,4,5// 3회전: 1,2,3,4,5// 4회전: 1,2,3,4,5
function solution(A) {    // Implement your solution here    const map = A.map((radius,j) =>  [- radius + j ,radius + j])    map.sort((a,b)=>a[0]- b[0])    let result =0;    for(let i=0; i<map.length; i++){        const mainRightRange= map[i][1]        for(let n=i+1; n< map.length; n++){            const left = map[n][0]            if(mainRightRange >= left)result++            else break            if(result > 10000000)return  -1        }    }    return result}
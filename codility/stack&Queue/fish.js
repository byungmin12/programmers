function solution(A, B) {    const stack = [[A[0], B[0]]]    for (let i = 1; i < A.length; i++) {        const weight = A[i]        const direction = B[i]        if (stack.length > 0) {            const [upStreamWeight, upStreamDirection] = stack[stack.length - 1]            if (direction === 0 && upStreamDirection === 1) {                if (upStreamWeight < weight) {                    stack.pop()                    i--                }            } else {                stack.push([weight, direction])            }        } else {            stack.push([weight, direction])        }    }    return stack.length}
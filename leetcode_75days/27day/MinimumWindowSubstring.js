/** * @param {string} s * @param {string} t * @return {string} */var minWindow = function(s, t) {    let m = s.length    let n = t.length;    if(m<n)return ""    if(m===n)return s    let min = Infinity    let checkObj = {}    new Array(n).fill(0).forEach((_,idx)=>{        checkObj[t[idx]] = false    })    // checkObj = { A: false, B: false, C: false }    for(let i =0; i<m;i++){        if(checkObj[s[i]] === undefined)continue;        let pointer = i;        let localCheck = n        let str = ""        while(true && i > m-n){            if(checkObj[s[pointer]] === false){                checkObj[s[pointer]] = true                localCheck--            }            str = str + s[pointer]            if(localCheck===0){                for(let k in checkObj){                    checkObj[k]= false                }                break            }            pointer++        }        min = Math.min(str.length, min)        i = i +2    }    // console.log(min)};
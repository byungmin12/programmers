/** * @param {number[]} nums * @param {number} target * @return {number} */var threeSumClosest = function(nums, target) {    // 투포인트 비교는 정렬이여야 가능    nums.sort((a,b)=>a-b)    let checkNum = Infinity    for(let i =0; i< nums.length-2; i++){        //nums.length -2까지 하는 이유는 right가 배열 마지막을 확인하기 때문에        let left= i+1 , right= nums.length -1            while(left<right){                let sum = nums[i] +nums[left] + nums[right]                //값이 target과 같으면 더 가까운 값을 찾을 수 없음                if(sum === target)return sum                // sum-target 과 checkNum -target 절대값을 비교해서 sum-target이 더 작다면                // checkNum에 새로운 값을 할당                if(Math.abs(sum-target) < Math.abs(checkNum - target)) checkNum = sum                //정렬이 된 상태니깐 sum이 target보다 크면 수를 줄여줘야하기에 빼줘야함                if(sum > target)right--                else left++            }    }    return checkNum};
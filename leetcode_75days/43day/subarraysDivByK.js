/** * @param {number[]} nums * @param {number} k * @return {number} */var subarraysDivByK = function(nums, k) {    const sums = Array(nums.length+1).fill(0)    for(let i=1; i<sums.length ; i++){        sums[i] = sums[i-1] + nums[i-1]    }    let cnt = 0    let sum = 0    let store = {"0":1}    for(let i =0; i<nums.length;i++){        // 4가 펄스면 4 + k도 펄스        // 5가 트루면 4+ k도 트루        // for(let n=i; n<sums.length; n++){        //     sums[n] = sums[n] - sums[i-1]        //     if(sums[n] % k ===0){        //         cnt++        //     }        // }        // const key = ((sums[i] % k) + k) % k        // if(store[key]=== undefined){        //     store[key] =1        // }else{        //     cnt = cnt + store[key]        //     store[key] = store[key] + 1        // }        sum = sum + nums[i]        let key = sum % k        if(key < 0)key= key+k        if(store[key] === undefined){            store[key] = 1        }else{            cnt = cnt + store[key]            store[key] = store[key]+1        }    }    return cnt};
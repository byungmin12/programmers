/** * @param {number} numCourses * @param {number[][]} prerequisites * @return {number[]} */var findOrder = function(numCourses, prerequisites) {    let needObj = {}    Array(numCourses).fill(null).forEach((el,idx)=>{        needObj[idx]= false    })    if(prerequisites.length ===0)return Object.keys(needObj)    prerequisites.forEach((el)=>{        needObj[el[0]] = !needObj[el[0]] ? [el[1]]: [...needObj[el[0]], el[1]]    })    console.log(needObj)    let result = []    let stack = [prerequisites.shift()]    while(stack.length){        let [a,b] = stack.pop()        if(needObj[b] === false){            result.push(b)            result.push(a)            needObj[a] = true        }else{            let idx = prerequisites.indexOf(a)            prerequisites =[...prerequisites.slice(0,4),...prerequisites.slice(5)]            stack.push(prerequisites[idx])        }    }    return result};
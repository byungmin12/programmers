/** * @param {string} s * @return {number} */// 00110var minFlipsMonoIncr = function(s) {    let one = 0    let result = 0    for(let i=0;i<s.length;i++){        if(s[i]==1){            one = one +1        }else{            result = Math.min(result + 1 , one)        }    }    return result};
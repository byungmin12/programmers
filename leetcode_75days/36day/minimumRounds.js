/** * @param {number[]} tasks * @return {number} */var minimumRounds = function(tasks) {    const store = tasks.reduce((acc,cur)=>{        if(acc[cur]){            acc[cur] = acc[cur] + 1        }else{            acc[cur] = 1        }        return acc    },{})    let minRounds = 0;    for (const count of Object.values(store)) {        if (count < 2) return -1;        minRounds += Math.ceil(count / 3);    }    return minRounds;};
// 43. Multiply Strings// Medium//// 5005//// 2074//// Add to List//// Share// Given two non-negative integers num1 and num2 represented as strings, return the product of num1 and num2, also represented as tapeEquilibrium.js string.////     Note: You must not use any built-in BigInteger library or convert the inputs to integer directly.////////     Example 1://// Input: num1 = "2", num2 = "3"// Output: "6"// Example 2://// Input: num1 = "123", num2 = "456"// Output: "56088"////// Constraints:////     1 <= num1.length, num2.length <= 200// num1 and num2 consist of digits only.//     Both num1 and num2 do not contain any leading zero, except the number 0 itself.//     Accepted// 563,841// Submissions// 1,466,292/** * @param {string} num1 * @param {string} num2 * @return {string} */var multiply = function(num1, num2) {    if (num1 === '0' || num2 === '0') return '0'    let resultArray = new Array(num1.length  + num2.length ).fill(0)    for(let i=num1.length -1; i>=0;i--){        for(let n=num2.length -1;n>=0; n--){            let sum = resultArray[i+n+1] + Number(num1[i]) * Number(num2[n])            resultArray[i+n+1] = sum%10            resultArray[i+n] += Math.floor(sum/10)        }    }    console.log(resultArray)    if (resultArray[0] === 0) resultArray.shift()    return resultArray.join('')    // return `${Number(resultArray.join(""))}`};
// you can write to stdout for debugging purposes, e.g.// console.log('this is a debug message');function solution(A) {    // Implement your solution here    let memo = {}    let max = Number.MIN_SAFE_INTEGER    const checkMaxiumValue = (value,columnNum,rowNum) => {        for(let i=0;i<A.length;i++){            if(i=== columnNum)continue            for(let n=0; n<A[columnNum].length; n++){                if(n=== rowNum || memo[A[i][n]])continue                max = Math.max(max, A[i][n]+value)            }        }        memo[value] = max        return max    }    for(let i=0;i<A.length;i++){        for(let n=0; n<A[i].length; n++){            checkMaxiumValue(A[i][n],i,n)        }    }    return max}
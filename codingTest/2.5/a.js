// you can write to stdout for debugging purposes, e.g.// console.log('this is tapeEquilibrium.js debug message');function solution(inner, outer, points_x, points_y) {    // Implement your solution here    const rangeOuter = Math.pow(outer,2)    const rangeInter = Math.pow(inner,2)    let result = 0    for(let i =0; i<points_x.length; i++){        const x = points_x[i]        const y = points_y[i]        const position =  Math.pow(x,2) + Math.pow(y,2)        if(rangeOuter > position && rangeInter < position){            result++        }    }    return result}
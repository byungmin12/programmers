/** * @param {string} text * @return {number} */var maxNumberOfBalloons = function(text) {    const hash = {b:0,a:0,l:0,l:0,o:0,o:0,n:0}    for(let i=0;i<text.length; i++){        if(hash[text[i]] !== undefined)hash[text[i]] = hash[text[i]] + 1    }    let cnt = 0    const b = "balloon"    while(true){        for(let i=0; i<b.length; i++){            if(hash[b[i]] <= 0){                return cnt            }else{                hash[b[i]]--            }        }        cnt++    }};법
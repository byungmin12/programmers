/** * @param {number[][]} matches * @return {number[][]} */var findWinners = function(m) {    let matches = m.reduce((acc,cur)=>{        acc[cur[0]] = acc[cur[0]]&& acc[cur[0]].wins ? {...acc[cur[0]] , wins : acc[cur[0]].wins + 1 }  : {...acc[cur[0]],wins: 1}        acc[cur[1]] = acc[cur[1]] && acc[cur[1]].lose ? {...acc[cur[1]], lose: acc[cur[1]].lose + 1} : {...acc[cur[1]],lose : 1}        return acc    },{})    let result = [[],[]]    for(matche in matches){        if(matches[matche].wins && !matches[matche].lose){            result[0].push(Number(matche))        }else if(matches[matche].lose === 1){            result[1].push(Number(matche))        }    }    return result};